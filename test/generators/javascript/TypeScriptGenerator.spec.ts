import { TypeScriptGenerator } from '../../../src/generators'; 

describe.skip('TypeScriptGenerator', function() {
  test('should `generate` function return OutputModels', async function() {
    // const generator = TypeScriptGenerator.createGenerator();

    // const doc: any = { $id: 'test' };
    // const models = await generator.generate(doc);

    // expect(models[0].result).toEqual("TypeScriptGenerator");
  });
});
