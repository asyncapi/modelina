# Generate models to files

A basic example how to generate the models to files.

## How to run this example
Run this example using:

```sh
npm i && npm run start
```
```sh
npm i && npm run start:windows
```